---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/main.py
  - src/cli/__init__.py
  - src/cli/commands.py
  - src/config/__init__.py
  - src/config/settings.py
autonomous: true

must_haves:
  truths:
    - "User runs `lead-gen --help` and sees documented command structure"
    - "User passes `--config` with API keys and tool loads config without errors"
  artifacts:
    - path: "src/main.py"
      provides: "CLI entry point with Click"
      exports: ["cli"]
    - path: "src/config/settings.py"
      provides: "Configuration loader supporting env/YAML/JSON"
      exports: ["Settings"]
    - path: "pyproject.toml"
      provides: "Project dependencies and metadata"
  key_links:
    - from: "src/main.py"
      to: "src/config/settings.py"
      via: "Settings class instantiation"
      pattern: "Settings\\("
---

<objective>
Set up CLI scaffold and configuration management system.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and dependencies</name>
  <files>pyproject.toml, src/__init__.py, src/main.py</files>
  <action>
    1. Create pyproject.toml with:
       - name: lead-gen
       - version: 0.1.0
       - dependencies: click>=8.0, pydantic>=2.0, pydantic-settings>=2.0, python-dotenv>=1.0, PyYAML>=6.0
       - entry point: lead-gen = src.main:cli
    2. Create src/__init__.py (empty)
    3. Create src/main.py as CLI entry point with:
       - @click.group() decorated cli() function
       - @click.version_option(version='0.1.0')
       - docstring: "Lead Generation CLI - Find and extract business contact information"
  </action>
  <verify>python -m pip install -e . && lead-gen --help</verify>
  <done>Project installs as `lead-gen` CLI and --help shows version</done>
</task>

<task type="auto">
  <name>Task 2: Create Click CLI with commands and help documentation</name>
  <files>src/cli/__init__.py, src/cli/commands.py</files>
  <action>
    1. Create src/cli/__init__.py with:
       - Import commands from commands.py
       - Add @cli.command() decorated functions for: search, scrape, init, export
       - Each command has @click.option() for relevant flags
       - Each command has help= parameter for documentation
    2. Create src/cli/commands.py with stub implementations for:
       - search: --query, --location, --limit flags
       - scrape: --url, --output flags  
       - init: creates default config file
       - export: --format (csv/json), --columns flags
    3. Import and register cli group in src/main.py
  </action>
  <verify>lead-gen --help && lead-gen search --help</verify>
  <done>All commands appear in --help with proper documentation</done>
</task>

<task type="auto">
  <name>Task 3: Create Pydantic Settings config loader</name>
  <files>src/config/__init__.py, src/config/settings.py</files>
  <action>
    1. Create src/config/__init__.py
    2. Create src/config/settings.py with:
       - Settings class extending BaseSettings
       - model_config: env_file='.env', extra='ignore'
       - Fields: serper_api_key, crawl4ai_timeout, requests_per_second, respect_robots_txt, output_format
       - from_file() classmethod supporting YAML/JSON loading
       - from_path() method to load config from file path (YAML/JSON takes precedence over env)
    3. Add __all__ exports
  </action>
  <verify>python -c "from src.config.settings import Settings; s = Settings(); print('Config loaded')"</verify>
  <done>Config loads from .env, YAML, and JSON without errors</done>
</task>

</tasks>

<verification>
- lead-gen --help shows all commands with documentation
- --config flag loads specified config file
- Settings from multiple sources (env, YAML, JSON) instantiate without errors
</verification>

<success_criteria>
CLI-01: Tool provides CLI with help documentation
CLI-02: Tool supports configuration via command-line flags
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>