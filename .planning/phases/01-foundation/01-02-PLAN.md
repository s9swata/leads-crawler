---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/__init__.py
  - src/core/rate_limiter.py
  - src/core/robots.py
  - src/core/types.py
autonomous: true

must_haves:
  truths:
    - "Tool respects robots.txt directives and logs compliance decisions"
    - "Tool enforces rate limits and logs throttling decisions"
    - "Tool validates lead data against schema and rejects invalid entries"
  artifacts:
    - path: "src/core/rate_limiter.py"
      provides: "Rate limiter with crawl-delay respect"
      exports: ["RateLimiter"]
    - path: "src/core/robots.py"
      provides: "robots.txt parser wrapper"
      exports: ["RobotsTxtParser"]
    - path: "src/core/types.py"
      provides: "Lead schema with validation"
      exports: ["Lead"]
  key_links:
    - from: "src/core/rate_limiter.py"
      to: "src/config/settings.py"
      via: "Settings instance for rate limit config"
      pattern: "Settings.*requests_per_second"
---

<objective>
Implement rate limiting, robots.txt parsing, and lead schema validation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create rate limiter with crawl-delay respect</name>
  <files>src/core/__init__.py, src/core/rate_limiter.py</files>
  <action>
    1. Create src/core/__init__.py with exports
    2. Create src/core/rate_limiter.py with:
       - RateLimiter class using asyncio
       - __init__(settings: Settings) - reads requests_per_second, respect_crawl_delay
       - acquire() async method - acquires permission to make request, respects crawl-delay
       - set_domain_delay(domain, delay) - sets per-domain crawl-delay from robots.txt
       - _log_throttle() - logs when throttling occurs
       - Uses sliding window algorithm for rate limiting
       - Logs: "Throttling request to {domain}, delay: {delay}s" when waiting
  </action>
  <verify>python -c "from src.core.rate_limiter import RateLimiter; from src.config.settings import Settings; r = RateLimiter(Settings()); print('RateLimiter created')"</verify>
  <done>Rate limiter instantiates and logs throttling decisions</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt parser wrapper</name>
  <files>src/core/robots.py</files>
  <action>
    1. Create src/core/robots.py with:
       - RobotsTxtParser class
       - __init__(settings: Settings) - stores respect_robots_txt setting
       - async can_fetch(url) -> bool - checks if URL is allowed
       - async get_crawl_delay(user_agent) -> Optional[float] - gets crawl-delay for user agent
       - _parse_robots_txt(url) - fetches and caches robots.txt
       - Logs: "Robots.txt: [ALLOW|BLOCK] {url}" for each URL check
       - Logs: "Crawl-delay for {user_agent}: {delay}s" when found
       - Uses protego library for parsing
  </action>
  <verify>python -c "from src.core.robots import RobotsTxtParser; from src.config.settings import Settings; r = RobotsTxtParser(Settings()); print('RobotsTxtParser created')"</verify>
  <done>Robots.txt parser instantiates and logs compliance decisions</done>
</task>

<task type="auto">
  <name>Task 3: Create Lead schema with validation</name>
  <files>src/core/types.py</files>
  <action>
    1. Create src/core/types.py with:
       - Lead class extending BaseModel
       - Fields: id, company_name, website, email, phone, linkedin, source, source_url, discovered_at, scraped_at
       - email: Optional[EmailStr] with validation
       - website: Optional[str] - validates URL format, adds https:// if missing
       - linkedin: Optional[str] - validates linkedin.com domain
       - phone: Optional[str] - normalizes to digits only
       - source: str - must be in ['search', 'scrape', 'manual']
       - discovered_at: datetime with default_factory=datetime.utcnow
       - ValidationError raised for invalid data
       - Model validates and rejects invalid entries
  </action>
  <verify>python -c "from src.core.types import Lead; from datetime import datetime; l = Lead(id='1', company_name='Test', source='search', source_url='http://test.com'); print('Lead created:', l.company_name)"</verify>
  <done>Lead schema validates and rejects invalid entries</done>
</task>

</tasks>

<verification>
- Rate limiter logs throttling decisions when requests exceed limit
- Robots.txt parser logs allow/block decisions for each URL
- Invalid Lead entries raise ValidationError
</verification>

<success_criteria>
DISC-04: Tool aggregates from free sources (rate limiting framework in place)
CLI-03: Tool displays progress during operations (logging)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>