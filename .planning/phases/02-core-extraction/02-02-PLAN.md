---
phase: 02-core-extraction
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extraction/adapters/__init__.py
  - src/extraction/adapters/base.py
  - src/extraction/adapters/crawl4ai_adapter.py
  - src/cli/commands.py
autonomous: true

must_haves:
  truths:
    - "Tool can extract website URLs from page content"
    - "CLI can invoke extraction pipeline with URL input"
    - "Source adapter interface enables swappable implementations"
  artifacts:
    - path: "src/extraction/adapters/base.py"
      provides: "Abstract base class for source adapters"
      exports: ["SourceAdapter"]
    - path: "src/extraction/adapters/crawl4ai_adapter.py"
      provides: "Crawl4ai implementation of SourceAdapter"
      exports: ["Crawl4aiAdapter"]
    - path: "src/cli/commands.py"
      provides: "scrape command using extraction pipeline"
      contains: "scrape"
---

<objective>
Implement source adapter interface (Strategy pattern) and integrate extraction pipeline with CLI.

Purpose: Enable swappable scraper implementations while maintaining common interface. Add scrape command to CLI.

Output:
- Source adapter base class (abstract)
- Crawl4ai adapter implementation
- scrape CLI command using extraction pipeline
</objective>

<execution_context>
@/Users/s4swata/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/s4swata/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-extraction/02-RESEARCH.md
@.planning/phases/02-core-extraction/02-01-PLAN.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source adapter interface</name>
  <files>src/extraction/adapters/__init__.py, src/extraction/adapters/base.py</files>
  <action>
    Create `src/extraction/adapters/` directory:
    - `src/extraction/adapters/__init__.py` - exports SourceAdapter, Crawl4aiAdapter
    - `src/extraction/adapters/base.py` - Abstract SourceAdapter class:
      - Abstract methods: async fetch(url) -> str, get_source_name() -> str
      - Concrete method: async fetch_if_allowed(url) -> str (uses robots)
      - Include rate_limiter integration as abstract method or mixin

    Follow Strategy pattern from research.
  </action>
  <verify>python -c "from src.extraction.adapters import SourceAdapter; print('OK')"</verify>
  <done>SourceAdapter abstract class exists with fetch methods</done>
</task>

<task type="auto">
  <name>Task 2: Implement Crawl4aiAdapter</name>
  <files>src/extraction/adapters/crawl4ai_adapter.py</files>
  <action>
    Create Crawl4aiAdapter class extending SourceAdapter:
    - Constructor takes AsyncWebCrawler instance
    - fetch(url) - uses crawler.arun() and returns HTML
    - get_source_name() - returns "crawl4ai"
    - Inherits fetch_if_allowed from base class (robots check)
  </action>
  <verify>python -c "from src.extraction.adapters import Crawl4aiAdapter; print('OK')"</verify>
  <done>Crawl4aiAdapter class is importable and functional</done>
</task>

<task type="auto">
  <name>Task 3: Add website URL extractor and integrate with CLI</name>
  <files>src/extraction/extractors/website.py, src/cli/commands.py</files>
  <action>
    Create `src/extraction/extractors/website.py`:
    - WebsiteExtractor class
    - extract_from_html(html) -> list[str]
    - Extract absolute URLs from page (filter for domain patterns)
    - Use BeautifulSoup to find all <a href> links

    Update `src/cli/commands.py`:
    - Add scrape command that takes URL argument
    - Uses Scraper + all extractors (email, phone, social, website)
    - Outputs extracted data to stdout or exports to Lead
    - Include --format flag (json, text)
  </action>
  <verify>poetry run lead-gen scrape --help</verify>
  <done>scrape command exists and accepts URL argument</done>
</task>

</tasks>

<verification>
- SourceAdapter is abstract with fetch, get_source_name methods
- Crawl4aiAdapter implements SourceAdapter
- WebsiteExtractor extracts URLs from HTML
- scrape CLI command available and functional
- Extraction pipeline works end-to-end: URL -> HTML -> extractors -> structured data
</verification>

<success_criteria>
1. User provides a URL and tool extracts website URLs from page
2. CLI scrape command works with URL input
3. Source adapter interface enables alternative scraper implementations
4. All extractors (email, phone, social, website) work together in pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-extraction/02-02-SUMMARY.md`
</output>
