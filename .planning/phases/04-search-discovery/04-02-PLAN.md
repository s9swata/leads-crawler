---
phase: 04-search-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "User sees progress updates during search"
    - "Tool discovers leads from multiple pages"
    - "Tool handles rate limits gracefully"
---

<objective>
Gap closure for Phase 4: progress updates, multi-page search, rate limit handling
</objective>

<tasks>
1. **Add progress indicator to search command**
   - artifact: src/cli/commands.py
   - issue: Lines 55-66 show simple echo statements, no progress/tqdm/rich output
   - action: ADD progress bar using `tqdm` or `rich` library around `adapter.search()` call at line 58
   - verification: Run `lead-gen search "test" --limit 10` and observe progress bar during API call

2. **Add pagination loop to SerperAdapter**
   - artifact: src/search/adapters/serper.py
   - issue: Line 15 sets num=limit but no page iteration. API supports 'start' parameter for pagination
   - action: ADD pagination logic using `start` parameter to fetch multiple pages until limit reached
   - verification: Run `lead-gen search "test" --limit 25` and verify >10 results returned from multiple pages

3. **Add rate limit handling with exponential backoff**
   - artifact: src/search/adapters/serper.py
   - issue: No rate limit response detection or retry logic
   - action: ADD 429 response detection and exponential backoff retry in `search()` method
   - verification: Simulate 429 response or check logs show retry attempts on rate limit
</tasks>
