---
phase: 05-resilience-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/retry.py
  - src/search/adapters/serper.py
  - src/extraction/adapters/crawl4ai.py
  - src/cli/commands.py
autonomous: true
must_haves:
  truths:
    - "All adapters retry failed operations with exponential backoff"
    - "User can preview operations with --dry-run"
    - "Dry-run shows what would happen without executing"
---

<objective>
Provide robust retry with exponential backoff across all adapters and add dry-run mode for previewing operations.
</objective>

<tasks>

<task type="auto">
  <name>Create generic retry decorator</name>
  <files>src/core/retry.py</files>
  <action>
    Create src/core/retry.py with:
    - @retry decorator function supporting both sync and async
    - Parameters: max_retries (default 3), initial_backoff (default 1.0), backoff_factor (default 2.0), 
      retry_on (tuple of exception types)
    - On retry: log attempt number and backoff duration
    - Use asyncio.sleep for async, time.sleep for sync
    - Include jitter for randomization
  </action>
  <verify>grep -n "@retry" src/core/retry.py && grep -n "def retry" src/core/retry.py</verify>
  <done>@retry decorator exists and supports both sync/async functions</done>
</task>

<task type="auto">
  <name>Apply retry to SerperAdapter</name>
  <files>src/search/adapters/serper.py</files>
  <action>
    Update SerperAdapter.search() method:
    - Replace custom retry logic with @retry decorator
    - Configure: max_retries=5, initial_backoff=1.0, backoff_factor=2.0
    - Retry on: httpx.HTTPStatusError (for 429, 5xx), httpx.ConnectError, httpx.TimeoutException
    - Log each retry attempt
  </action>
  <verify>grep -n "@retry" src/search/adapters/serper.py</verify>
  <done>SerperAdapter uses @retry decorator for all API calls</done>
</task>

<task type="auto">
  <name>Apply retry to Crawl4aiAdapter</name>
  <files>src/extraction/adapters/crawl4ai.py</files>
  <action>
    Update Crawl4aiAdapter.fetch_if_allowed() method:
    - Add @retry decorator
    - Configure: max_retries=3, initial_backoff=2.0, backoff_factor=2.0
    - Retry on: network errors, timeouts
  </action>
  <verify>grep -n "@retry" src/extraction/adapters/crawl4ai.py 2>/dev/null || echo "File may not exist"</verify>
  <done>Crawl4aiAdapter uses @retry decorator for fetch operations</done>
</task>

<task type="auto">
  <name>Add --dry-run to search command</name>
  <files>src/cli/commands.py</files>
  <action>
    Update search command:
    - Add click option: @click.option('--dry-run', is_flag=True, default=False, help="Preview without executing")
    - In _search function: if dry_run, print query, location, limit, expected results count
    - Show "Would search for: {query}" without calling API
    - Exit early with code 0
  </action>
  <verify>grep -n "dry-run\|dry_run" src/cli/commands.py</verify>
  <done>Search command shows preview when --dry-run is used</done>
</task>

<task type="auto">
  <name>Add --dry-run to scrape command</name>
  <files>src/cli/commands.py</files>
  <action>
    Update scrape command:
    - Add @click.option('--dry-run', is_flag=True, default=False)
    - In _scrape function: if dry_run, print URL and extractors
    - Show "Would scrape: {url} with extractors [email, phone, social, website]"
    - Exit early without calling crawler
  </action>
  <verify>grep -n "Would scrape" src/cli/commands.py</verify>
  <done>Scrape command shows preview when --dry-run is used</done>
</task>

</tasks>

<verification>
- poetry run python -m src.main search --query "test" --dry-run → shows preview without API call
- poetry run python -m src.main scrape --url "https://example.com" --dry-run → shows preview without scraping
- Simulate network failure → verify retry attempts logged
</verification>

<success_criteria>
All operations retry on failure, dry-run previews work without side effects
</success_criteria>
