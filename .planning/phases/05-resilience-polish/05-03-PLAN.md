---
phase: 05-resilience-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/cli/commands.py
  - src/cli/errors.py
  - src/core/signals.py
autonomous: true
must_haves:
  truths:
    - "User sees friendly error messages instead of stack traces"
    - "User can exit cleanly with Ctrl+C and save progress"
    - "Progress bars clean up properly on exit"
---

<objective>
Provide user-friendly error messages for common failures and ensure graceful shutdown on interrupts (Ctrl+C).
</objective>

<tasks>

<task type="auto">
  <name>Create custom exception classes</name>
  <files>src/cli/errors.py</files>
  <action>
    Create src/cli/errors.py with custom exceptions:
    - APIKeyError: "API key not configured. Set {key_name} in your .env file"
    - NetworkError: "Network error. Check your connection and retry."
    - RateLimitError: "Rate limited. Wait {seconds} seconds before retrying."
    - ScrapingError: "Failed to scrape {url}: {reason}"
    - ConfigurationError: "Configuration error: {detail}"
    
    Each should:
    - Inherit from LeadGenError (base class)
    - Have user_message property for CLI output
    - Have technical_details property for debug mode
  </action>
  <verify>grep -n "class.*Error" src/cli/errors.py</verify>
  <done>Custom exception hierarchy exists with user-friendly messages</done>
</task>

<task type="auto">
  <name>Add signal handlers for graceful shutdown</name>
  <files>src/core/signals.py</files>
  <action>
    Create src/core/signals.py with:
    - setup_signal_handlers(callback): registers SIGINT/SIGTERM handlers
    - On signal received: call callback (if provided), then exit cleanly
    - Use tqdm.auto() for progress bar cleanup
    - Store cleanup callback reference for access from commands
    
    Example:
    ```python
    cleanup_callback = None
    def handle_signal(signum, frame):
        if cleanup_callback:
            cleanup_callback()
        click.echo("\nInterrupted. Progress saved.")
        sys.exit(0)
    ```
  </action>
  <verify>grep -n "signal.signal\|SIGINT\|SIGTERM" src/core/signals.py</verify>
  <done>Signal handlers save checkpoint and exit cleanly on Ctrl+C</done>
</task>

<task type="auto">
  <name>Update CLI error handling</name>
  <files>src/cli/commands.py</files>
  <action>
    Update _search and _scrape functions:
    - Wrap main logic in try/except blocks
    - Catch APIKeyError → click.echo with styled red message, exit code 1
    - Catch RateLimitError → show wait message, continue
    - Catch NetworkError → click.echo "Network error", exit code 1
    - Catch ScrapingError → log failed URL, continue with remaining
    - Use click.style() for colored output (red for errors, yellow for warnings)
    - In finally: ensure progress bars are closed (tqdm.close())
  </action>
  <verify>grep -n "except.*Error" src/cli/commands.py | head -5</verify>
  <done>CLI shows friendly messages instead of Python tracebacks</done>
</task>

<task type="auto">
  <name>Integrate signal handlers with commands</name>
  <files>src/cli/commands.py</files>
  <action>
    In _search and _scrape:
    - Import setup_signal_handlers from src.core.signals
    - Before main loop: setup_signal_handlers(checkpoint_callback)
    - Set global cleanup_callback to save checkpoint
    - On KeyboardInterrupt: save checkpoint, show "Progress saved", exit
    
    Example integration:
    ```python
    from src.core.signals import setup_signal_handlers
    
    async def _search(...):
        checkpoint_svc = CheckpointService()
        setup_signal_handlers(lambda: checkpoint_svc.save_progress(...))
        ...
    ```
  </action>
  <verify>grep -n "setup_signal_handlers" src/cli/commands.py</verify>
  <done>Signal handlers properly integrated with search/scrape commands</done>
</task>

</tasks>

<verification>
- poetry run python -m src.main search --query "test" (no API key) → shows friendly "API key not configured" message
- Run search, press Ctrl+C → saves checkpoint, shows "Progress saved", exits cleanly
- poetry run python -m src.main scrape --url "https://invalid-domain-that-does-not-exist.xyz" → shows friendly error
</verification>

<success_criteria>
User experiences friendly errors and clean shutdown, never sees raw Python tracebacks
</success_criteria>
