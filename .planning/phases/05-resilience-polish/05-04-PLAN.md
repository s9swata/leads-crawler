---
phase: 05-resilience-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands.py
  - src/storage/checkpoint.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Interrupted checkpoints are resumable"
    - "CheckpointStatus enum matches signal handler usage"
---

<objective>
Fix checkpoint resume after Ctrl+C: signal handler saves "interrupted" status but CheckpointStatus enum only has PENDING/RUNNING/COMPLETED/FAILED.
</objective>

<tasks>

<task type="auto">
  <name>Fix CheckpointStatus enum and is_resumable</name>
  <files>src/storage/checkpoint.py</files>
  <action>
    Update CheckpointStatus enum to include INTERRUPTED status.
    Update is_resumable() to also check for INTERRUPTED status.
  </action>
  <verify>grep -n "INTERRUPTED" src/storage/checkpoint.py</verify>
  <done>INTERRUPTED status exists and is_resumable() returns True for it</done>
</task>

<task type="auto">
  <name>Verify signal handler uses correct status</name>
  <files>src/cli/commands.py</files>
  <action>
    Check signal handler callbacks use INTERRUPTED status consistently.
    If they use "interrupted" string, update to use CheckpointStatus.INTERRUPTED.
  </action>
  <verify>grep -n "interrupted\|INTERRUPTED" src/cli/commands.py</verify>
  <done>Signal handler uses CheckpointStatus.INTERRUPTED enum value</done>
</task>

</tasks>

<verification>
- Run search, press Ctrl+C, re-run - should detect interrupted checkpoint and resume
- Check checkpoint status is saved as INTERRUPTED
- Verify is_resumable() returns True for INTERRUPTED checkpoints
</verification>
