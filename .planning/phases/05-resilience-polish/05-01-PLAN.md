---
phase: 05-resilience-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/models.py
  - src/storage/checkpoint.py
  - src/cli/commands.py
autonomous: true
must_haves:
  truths:
    - "User can resume interrupted search from checkpoint"
    - "User can resume interrupted scrape from checkpoint"
    - "Checkpoint saves progress after each page/URL"
---

<objective>
Enable users to resume interrupted search/scrape operations from the last saved checkpoint, preventing data loss on failures.
</objective>

<tasks>

<task type="auto">
  <name>Add Checkpoint model</name>
  <files>src/storage/models.py</files>
  <action>
    Add Checkpoint SQLAlchemy model to models.py:
    - Fields: id (UUID), job_type (string), job_id (string), status (enum: pending/running/completed/failed), 
      completed_items (JSON), failed_items (JSON), created_at (datetime), updated_at (datetime)
    - Create unique index on (job_type, job_id)
  </action>
  <verify>grep -n "class Checkpoint" src/storage/models.py</verify>
  <done>Checkpoint model exists with all required fields</done>
</task>

<task type="auto">
  <name>Create CheckpointService</name>
  <files>src/storage/checkpoint.py</files>
  <action>
    Create src/storage/checkpoint.py with CheckpointService class:
    - save_progress(job_type, job_id, completed, failed): saves checkpoint with JSON data
    - load_checkpoint(job_type, job_id): returns checkpoint or None
    - clear_checkpoint(job_type, job_id): removes checkpoint
    - is_resumable(job_type, job_id): checks if checkpoint exists and is resumable
    Use existing session_scope from database.py
  </action>
  <verify>grep -n "class CheckpointService" src/storage/checkpoint.py</verify>
  <done>CheckpointService exists with save/load/clear/is_resumable methods</done>
</task>

<task type="auto">
  <name>Integrate checkpoint into search command</name>
  <files>src/cli/commands.py</files>
  <action>
    Update _search function in commands.py:
    - Generate job_id from query hash + timestamp
    - Before API call, check for existing checkpoint via CheckpointService.is_resumable()
    - If resumable, load checkpoint and skip already-completed pages
    - After each page, save progress via CheckpointService.save_progress()
    - Show "Resuming from checkpoint..." message if resuming
    - On completion, clear checkpoint
  </action>
  <verify>grep -n "CheckpointService" src/cli/commands.py</verify>
  <done>Search command saves/loads checkpoints properly</done>
</task>

<task type="auto">
  <name>Integrate checkpoint into scrape command</name>
  <files>src/cli/commands.py</files>
  <action>
    Update _scrape function in commands.py:
    - Use URL as job_id
    - Before scraping, check for existing checkpoint
    - Save progress after each successful scrape
    - On re-run, resume from last completed URL
    - Show "Resuming scrape from {n} completed URLs..."
  </action>
  <verify>grep -n "checkpoint" src/cli/commands.py | head -5</verify>
  <done>Scrape command saves/loads checkpoints properly</done>
</task>

</tasks>

<verification>
- Run partial search, interrupt with Ctrl+C, re-run - should resume from checkpoint
- Run partial scrape, interrupt, re-run - should resume from last URL
- Verify checkpoint cleared after successful completion
</verification>

<success_criteria>
User can resume interrupted operations without data loss
</success_criteria>
