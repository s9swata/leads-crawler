---
phase: 03-persistence-export
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/lead_ingestion.py
  - src/cli/commands.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User runs search twice with same query, sees no duplicate leads"
    - "Scraped leads are persisted to database with deduplication"
    - "Deduplicator.find_duplicate() is invoked on new leads"
  artifacts:
    - path: "src/storage/lead_ingestion.py"
      provides: "LeadIngestionService combining deduplication + storage"
      contains: "class LeadIngestionService"
      exports: ["LeadIngestionService"]
    - path: "src/cli/commands.py"
      provides: "scrape command stores results to database"
      contains: "LeadIngestionService"
  key_links:
    - from: "src/cli/commands.py"
      to: "src/storage/lead_ingestion.py"
      via: "import and instantiate LeadIngestionService"
      pattern: "from src.storage.lead_ingestion import"
    - from: "src/storage/lead_ingestion.py"
      to: "src/storage/deduplicator.py"
      via: "calls Deduplicator.find_duplicate()"
      pattern: "Deduplicator.*find_duplicate"
    - from: "src/storage/lead_ingestion.py"
      to: "src/storage/lead_repo.py"
      via: "calls LeadRepository.add()"
      pattern: "repo\\.add\\("
---

<objective>
Close gap: User runs search twice with same query, sees no duplicate leads.

Purpose: Create lead ingestion workflow that takes scraped leads, checks for duplicates, and stores unique leads to database.

Output: Working deduplication when running scrape command.
</objective>

<execution_context>
@/Users/s4swata/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/s4swata/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/storage/deduplicator.py - Deduplicator class with find_duplicate() method
@src/storage/lead_repo.py - LeadRepository with add() method  
@src/core/types.py - Lead Pydantic model with validation
@src/cli/commands.py - Current scrape command (lines 36-99) that doesn't store results

Gap reason from VERIFICATION.md: "Deduplicator exists in src/storage/deduplicator.py but is never called. No workflow connects search/scrape results to LeadRepository.add()."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LeadIngestionService</name>
  <files>src/storage/lead_ingestion.py</files>
  <action>
    Create src/storage/lead_ingestion.py with LeadIngestionService class that:
    - Takes scraped data dict (url, emails, phones, social, websites)
    - Creates Lead objects using src.core.types.Lead model
    - Uses Deduplicator.find_duplicate() to check for existing leads
    - Stores unique leads via LeadRepository.add()
    - Returns tuple of (added_count, duplicate_count, leads)
    
    Use session_scope from src.storage.database to manage DB transactions.
    Import Lead from src.core.types, Deduplicator from src.storage.deduplicator, LeadRepository from src.storage.lead_repo.
  </action>
  <verify>Python import check: python -c "from src.storage.lead_ingestion import LeadIngestionService; print('OK')"</verify>
  <done>LeadIngestionService can ingest leads with deduplication</done>
</task>

<task type="auto">
  <name>Task 2: Update scrape command to store results</name>
  <files>src/cli/commands.py</files>
  <action>
    Modify src/cli/commands.py:
    1. Add import: from src.storage.lead_ingestion import LeadIngestionService
    2. In _scrape() async function, after extracting data (lines 74-77), add:
       - Create LeadIngestionService instance
       - Call service.ingest() with scraped data
       - Print result: "Stored {added} new leads, {duplicates} duplicates skipped"
    
    Pass company_name as None or extract from URL domain. Use source="scrape".
  </action>
  <verify>Check scrape command shows no syntax errors: python -c "from src.cli.commands import scrape; print('OK')"</verify>
  <done>Scrape command stores results to database with deduplication</done>
</task>

<task type="auto">
  <name>Task 3: Test deduplication workflow</name>
  <files></files>
  <action>
    Test the complete workflow:
    1. Run scrape on a URL (e.g., example.com)
    2. Run scrape on same URL again
    3. Verify second run shows "1 duplicates skipped" or similar
    
    Use: python -m src.cli.commands scrape --url https://example.com
  </action>
  <verify>Second scrape of same URL reports duplicate, leads table has only 1 record</verify>
  <done>User runs scrape twice with same URL, sees no duplicate leads stored</done>
</task>

</tasks>

<verification>
Run scrape command twice on same URL. Check that:
- First run: "Stored 1 new leads" (or more based on data found)
- Second run: Reports duplicates and doesn't increase lead count
- leads command shows only unique leads
</verification>

<success_criteria>
- LeadIngestionService created and functional
- Scrape command stores leads to database
- Deduplication works: running scrape twice on same URL doesn't create duplicates
- VERIFICATION.md requirement 1 now passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-export/03-03-SUMMARY.md`
</output>
