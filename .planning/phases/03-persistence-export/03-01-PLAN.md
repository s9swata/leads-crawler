---
phase: 03-persistence-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/__init__.py
  - src/storage/database.py
  - src/storage/models.py
  - src/storage/lead_repo.py
  - src/storage/deduplicator.py
  - pyproject.toml
autonomous: true
must_haves:
  truths:
    - User can store leads between runs
    - Duplicate leads are automatically removed
    - Queries on leads are fast due to indexes
  artifacts:
    - path: "src/storage/database.py"
      provides: "SQLAlchemy database setup and session management"
    - path: "src/storage/models.py"
      provides: "SQLAlchemy Lead model matching Lead schema"
    - path: "src/storage/lead_repo.py"
      provides: "Lead CRUD operations with repository pattern"
    - path: "src/storage/deduplicator.py"
      provides: "Email/domain deduplication logic"
    - path: "pyproject.toml"
      provides: "SQLAlchemy dependency"
  key_links:
    - from: "src/storage/lead_repo.py"
      to: "src/storage/models.py"
      via: "import Lead from models"
    - from: "src/storage/deduplicator.py"
      to: "src/storage/lead_repo.py"
      via: "uses LeadRepository to check duplicates before insert"
---

<objective>
Create lead storage infrastructure with SQLite, SQLAlchemy Core, deduplication engine, and indexes for query performance.
</objective>

<execution_context>
@/Users/s4swata/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/s4swata/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 3 requirements)
@.planning/phases/03-persistence-export/03-RESEARCH.md (SQLAlchemy patterns)
@.planning/PROJECT.md
@src/core/types.py (existing Lead schema from Phase 1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLAlchemy dependency and create storage module</name>
  <files>pyproject.toml, src/storage/__init__.py, src/storage/database.py</files>
  <action>
    1. Add SQLAlchemy 2.x to pyproject.toml dependencies
    2. Create src/storage/__init__.py exporting database functions
    3. Create src/storage/database.py with:
       - SQLAlchemy engine (sqlite:///leads.db, echo=False)
       - MetaData object
       - get_session() function returning Session context manager
       - init_db() function to create tables
  </action>
  <verify>poetry install completes without error</verify>
  <done>Database engine created, get_session() returns working SQLAlchemy Session</done>
</task>

<task type="auto">
  <name>Task 2: Create SQLAlchemy Lead model</name>
  <files>src/storage/models.py</files>
  <action>
    Create src/storage/models.py with Lead SQLAlchemy model:
    - Use sqlalchemy.Column with appropriate types (String, DateTime)
    - Map to existing Lead schema fields: id, company_name, email, website, phone, linkedin, source, source_url, discovered_at, scraped_at
    - Use existing src/core/types.py Lead as reference for field types
    - Add indexes on: email, website, source, discovered_at for query performance
  </action>
  <verify>Python can import models without error: python -c "from src.storage.models import Lead"</verify>
  <done>Lead model created with all fields and indexes matching Lead schema</done>
</task>

<task type="auto">
  <name>Task 3: Create LeadRepository with CRUD operations</name>
  <files>src/storage/lead_repo.py</files>
  <action>
    Create src/storage/lead_repo.py with LeadRepository class:
    - __init__(session: Session)
    - add(lead: Lead) -> Lead: Insert lead, return with ID
    - find_by_email(email: str) -> Optional[Lead]
    - find_by_website(url: str) -> Optional[Lead]
    - list_all(limit: int, offset: int) -> list[Lead]
    - count() -> int: Total lead count
    - _row_to_lead(row) -> Lead: Convert SQLAlchemy row to Lead
  </action>
  <verify>LeadRepository can be instantiated and CRUD methods work</verify>
  <done>LeadRepository provides all CRUD operations for leads</done>
</task>

<task type="auto">
  <name>Task 4: Create Deduplicator for email/domain matching</name>
  <files>src/storage/deduplicator.py</files>
  <action>
    Create src/storage/deduplicator.py with Deduplicator class:
    - normalize_email(email: str) -> Optional[str]: lowercase + strip
    - extract_domain(url: str) -> Optional[str]: use urllib.parse, strip www.
    - is_duplicate(lead: Lead, existing: list[Lead]) -> bool: match on email OR domain
    - find_duplicate(lead: Lead, repo: LeadRepository) -> Optional[Lead]: query repo for existing
    Use email domain match OR website domain match for deduplication.
  </action>
  <verify>Deduplicator correctly identifies duplicates by email or domain</verify>
  <done>Deduplicator can detect duplicate leads before insertion</done>
</task>

</tasks>

<verification>
- [ ] leads.db created in project root after first write
- [ ] Lead model columns match src/core/types.py Lead schema
- [ ] Indexes created on email, website, source, discovered_at
- [ ] Deduplicator identifies same company with different email as duplicate
</verification>

<success_criteria>
User can store leads between CLI runs with automatic deduplication
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-export/03-01-SUMMARY.md`
</output>
