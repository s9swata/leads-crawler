---
phase: 03-persistence-export
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/storage/query_builder.py
  - src/export/__init__.py
  - src/export/csv_generator.py
  - src/export/columns.py
  - src/cli/commands.py
autonomous: true
must_haves:
  truths:
    - User can filter leads by data completeness (has email, has phone)
    - User can sort leads by source or timestamp
    - User can view leads with pagination
    - User can export leads to CSV with column selection
    - User can select specific columns and CSV contains only selected
  artifacts:
    - path: "src/storage/query_builder.py"
      provides: "Filter and sort query builder"
    - path: "src/export/csv_generator.py"
      provides: "CSV export with column selection"
    - path: "src/export/columns.py"
      provides: "Column definitions and mapping"
    - path: "src/cli/commands.py"
      provides: "leads and export CLI commands"
  key_links:
    - from: "src/cli/commands.py"
      to: "src/storage/query_builder.py"
      via: "uses query builder for filtering/sorting"
    - from: "src/cli/commands.py"
      to: "src/export/csv_generator.py"
      via: "calls export_csv with leads and column list"
---

<objective>
Create filter/sort engine, CSV generator with column selection, and CLI commands for viewing and exporting leads.
</objective>

<execution_context>
@/Users/s4swata/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/s4swata/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 3 requirements: LEAD-01, LEAD-03, LEAD-04, EXPT-01, EXPT-02, EXPT-03)
@.planning/phases/03-persistence-export/03-RESEARCH.md (CSV export patterns)
@.planning/phases/03-persistence-export/03-01-PLAN.md (storage infrastructure from 03-01)
@.planning/phases/03-persistence-export/03-01-SUMMARY.md (may not exist yet - 03-01 is wave 1)
@src/storage/lead_repo.py
@src/core/types.py (Lead schema)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filter and sort query builder</name>
  <files>src/storage/query_builder.py</files>
  <action>
    Create src/storage/query_builder.py with build_leads_query function:
    - Parameters: has_email, has_phone, has_website, source, sort_by, sort_order, limit, offset
    - Use SQLAlchemy select with where conditions for filters
    - Apply sorting with desc/asc on sort_by column
    - Apply pagination with limit/offset
    - Return list[Lead]
    Support filters: has_email (bool), has_phone (bool), has_website (bool), source (str)
    Support sort_by: discovered_at, source, company_name, scraped_at
  </action>
  <verify>Query builder returns filtered/sorted leads from database</verify>
  <done>Query builder supports all filter and sort options from requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create CSV generator with column selection</name>
  <files>src/export/columns.py, src/export/csv_generator.py</files>
  <action>
    1. Create src/export/columns.py with:
       - LeadColumns enum (id, company_name, email, website, phone, linkedin, source, source_url, discovered_at, scraped_at)
       - COLUMN_MAPPING dict mapping friendly names to field names
       - AVAILABLE_COLUMNS list of all available columns
    2. Create src/export/csv_generator.py with:
       - export_csv(leads: list[Lead], path: str, columns: Optional[list[str]] = None) -> int
       - Default columns: company_name, email, website, phone, source, discovered_at
       - Use Python csv.DictWriter with utf-8 encoding
       - Return count of exported rows
  </action>
  <verify>CSV file created and opens correctly in Excel/Google Sheets</verify>
  <done>CSV export works with column selection</done>
</task>

<task type="auto">
  <name>Task 3: Add leads view and export CLI commands</name>
  <files>src/cli/commands.py</files>
  <action>
    Add to src/cli/commands.py:
    1. 'leads' command with options:
       --page, -p (default 1): Page number
       --per-page, -n (default 20): Items per page
       --filter-email/--no-email: Filter by email presence
       --filter-phone/--no-phone: Filter by phone presence
       --filter-website/--no-website: Filter by website presence
       --source: Filter by source
       --sort: Sort field (discovered_at, source, company_name, scraped_at)
       --order: Sort order (asc, desc)
       Displays leads in formatted table with pagination
    2. 'export' command with options:
       --output, -o: Output CSV file path (required)
       --columns: Comma-separated column names (optional, default all)
       --filter-*: Same filters as leads command
       Uses csv_generator.export_csv()
  </action>
  <verify>lead-gen leads --help and lead-gen export --help show correct options</verify>
  <done>User can view leads with filtering/sorting/pagination and export to CSV</done>
</task>

</tasks>

<verification>
- [ ] leads command filters by has_email, has_phone, has_website, source
- [ ] leads command sorts by discovered_at, source, company_name, scraped_at
- [ ] leads command paginates with --page and --per-page
- [ ] export command creates valid CSV file
- [ ] export command respects --columns selection
- [ ] CSV opens correctly in Excel/Google Sheets (UTF-8 encoding)
</verification>

<success_criteria>
User can view, filter, sort, and export leads with column selection
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-export/03-02-SUMMARY.md`
</output>
